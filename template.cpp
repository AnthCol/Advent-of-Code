#include <bits/stdc++.h>
#define pb push_back
using namespace std; 

int main(void){
    FILE * fptr = fopen("input.txt", "r"); 
    char * string = (char*)malloc(sizeof(char) * 10000); 
    int index = 0, sum = 0; 
    vector<int> vec; 
 
    while (!feof(fptr)){
        
    }


    printf(" ", ); 


    fclose(fptr); 
    free(string); 


    return 0; 
}